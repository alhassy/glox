description: Block comment between unary operator and operand

input: |-
  ! /* hello world */ true;

expectations:
  parsing: |-
    - Statement(ExprStatement(Op(
      BooleanNegation,
      [
        Literal(
          Boolean(True),
          Span(1, 21, 4),
        ),
      ],
      Span(1, 1, 20),
    )))
