description: Block comments

input: |-
  1 /* inline */ + /* another */ 2;

expectations:
  parsing: |-
    - Statement(ExprStatement(Op(
      Plus,
      [
        Literal(Number(1.0), Span(1, 1, 1)),
        Literal(Number(2.0), Span(1, 32, 1)),
      ],
      Span(1, 1, 32),
    )))
