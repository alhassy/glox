description: Equality

input: |-
  1 == 2;

expectations:
  parsing: |-
    - Statement(ExprStatement(Op(
      Equals,
      [
        Literal(Number(1.0), Span(1, 1, 1)),
        Literal(Number(2.0), Span(1, 6, 1)),
      ],
      Span(1, 1, 6),
    )))
