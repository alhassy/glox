description: Runtime error - negate string

input: |-
  print -"hello";

expectations:
  parsing: |-
    - Statement(Print(Op(
      NumericNegation,
      [
        Literal(
          String("hello"),
          Span(1, 8, 7),
        ),
      ],
      Span(1, 7, 1),
    )))
  evaluation: |-
    ERROR ┌─ Runtime error at line 1, columns 8-14
      |
    1 | print -"hello";
      |        ^^^^^^^
      |     Cannot apply '-' to string; expected number
