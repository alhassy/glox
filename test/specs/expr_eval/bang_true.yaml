description: Boolean negation - !true

input: |-
  print !true;

expectations:
  parsing: |-
    - Statement(Print(Op(
      BooleanNegation,
      [
        Literal(
          Boolean(True),
          Span(1, 8, 4),
        ),
      ],
      Span(1, 7, 1),
    )))
  evaluation: |-
    PRINTED False
