description: At least (>=) - false

input: |-
  print 2 >= 3;

expectations:
  parsing: |-
    - Statement(Print(Op(
      AtLeast,
      [
        Literal(Number(2.0), Span(1, 7, 1)),
        Literal(Number(3.0), Span(1, 12, 1)),
      ],
      Span(1, 7, 6),
    )))
  evaluation: |-
    PRINTED False
