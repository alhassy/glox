description: Boolean negation - !false

input: |-
  print !false;

expectations:
  parsing: |-
    - Statement(Print(Op(
      BooleanNegation,
      [
        Literal(
          Boolean(False),
          Span(1, 8, 5),
        ),
      ],
      Span(1, 7, 1),
    )))
  evaluation: |-
    PRINTED True
