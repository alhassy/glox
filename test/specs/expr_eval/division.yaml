description: Division

input: |-
  print 8 / 2;

expectations:
  parsing: |-
    - Statement(Print(Op(
      Divides,
      [
        Literal(Number(8.0), Span(1, 7, 1)),
        Literal(Number(2.0), Span(1, 11, 1)),
      ],
      Span(1, 7, 5),
    )))
  evaluation: |-
    PRINTED 4
